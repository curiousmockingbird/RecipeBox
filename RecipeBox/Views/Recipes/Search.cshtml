@{
	Layout = "_Layout";
}
@using RecipeBox.Models;
<h2>Results for @ViewBag.SearchQuery:</h2>

@if(@Model.Count == 0)
{
  <p>No recipes found</p>
}

<ul>
	@foreach (Recipe recipe in Model)
	{
		<li>@Html.ActionLink($"{recipe.Name}", "Details", "Recipes", new {id=recipe.RecipeId})</li>
	}
</ul>